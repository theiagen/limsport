{
  "name": "limsport-config-builder",
  "private": true,
  "type": "module",
  "description": "Browser-based config.yaml builder for LIMSport. No dependencies -- the browser page is vanilla HTML/CSS/JS, and tests use only Node's built-in test runner plus the real limsport Python package for validation.",
  "scripts": {
    "test": "node --test tests/"
  }
}
